spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/decoraigh_db
    username: postgres
    password: 2308
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080