spring:
  application:
    name: shop-sourcing-service
  datasource:
    url: jdbc:postgresql://localhost:5432/decoraigh_db
    username: postgres
    password: Mymombaby
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8082